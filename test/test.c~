#include <stdio.h>
#include "../main.h"
int main(void)
{
	printf("this is a string\n");
	printf("this is a string\n");
	char alpha = 'a';
	char string[] = "this is a string";
	printf("this is letter %c\n", alpha);
	_printf("this is letter %c\n", alpha);
	printf("%s", string);
	_printf("%s", string);
	return (0);
}
